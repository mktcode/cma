# Agent instructions

Keep code modular and decoupled. Avoid monolithic code. Write rudimentary comments to help navigate the codebase. Avoid writing comments that explain what the code does, but rather why it does it.

Read files in `./specs` if applicable.